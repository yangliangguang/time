import { VerticalBox, HorizontalBox } from "std-widgets.slint";

export component SimpleMenu inherits Window {
    width: 200px;
    height: 200px;
    
    VerticalBox {
        Text {
            text: "Hello Menu";
        }
    }
}